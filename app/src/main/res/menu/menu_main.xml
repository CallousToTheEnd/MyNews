<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/main_menu_search"
        android:icon="@mipmap/ic_main_menu_search"
        android:title="@string/main_menu__search"
        app:showAsAction="always" />

    <item
        android:id="@+id/main_menu_info"
        android:icon="@mipmap/ic_main_menu_info"
        android:title="@string/main_menu_info"
        app:showAsAction="always" />

</menu>
